# MySQL Database Configuration Example
# Copy to .env and update with your MySQL credentials

# MySQL connection string format:
DATABASE_URL=mysql+pymysql://username:password@localhost:3306/arc_pdf_tool

# Example for local MySQL:
# DATABASE_URL=mysql+pymysql://root:mypassword@localhost:3306/arc_pdf_tool

# Example for remote MySQL:
# DATABASE_URL=mysql+pymysql://arc_user:secretpass@mysql.example.com:3306/arc_pdf_tool

# For PostgreSQL (already supported):
# DATABASE_URL=postgresql://username:password@localhost:5432/arc_pdf_tool

# For SQLite (default, no setup required):
# DATABASE_URL=sqlite:///price_books.db

# Other settings (same for all databases)
SECRET_KEY=your-secret-key-here
BASEROW_API_TOKEN=your-baserow-token-here
BASEROW_BASE_URL=https://api.baserow.io
