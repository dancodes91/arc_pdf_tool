{
  "name": "arc-pdf-tool",
  "version": "1.0.0",
  "description": "PDF Price Book Parser with comprehensive UI",
  "scripts": {
    "test": "playwright test",
    "test:ui": "playwright test --ui",
    "test:headed": "playwright test --headed",
    "test:api": "playwright test test_api_endpoints",
    "test:ui-pages": "playwright test test_ui_pages",
    "test:debug": "playwright test --debug",
    "test:report": "playwright show-report"
  },
  "devDependencies": {
    "@playwright/test": "^1.40.0",
    "@types/node": "^20.0.0"
  }
}
